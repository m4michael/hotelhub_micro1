server:
  port: 8086
  
spring:
  application: 
    name: configServer
  cloud:
    config:
      server:
        git:
          uri: https://github.com/m4michael/microservConfig
          clone-on-start: true
          